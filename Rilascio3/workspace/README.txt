Il workspace qui presente contiene l'ultima versione del nostro applicativo.

In particolare, all'interno del progetto DAO, è stato inserito un package contenente i test in JUnit per verificare la correttezza delle funzionalità sviluppate.